{
    "name": "airtable_extract_table_data",
    "description": "Extract data from an Airtable base including records, fields, and metadata",
    "parameters": {
      "type": "object",
      "properties": {
        "maxRecords": {
          "type": "number",
          "description": "Maximum number of records to extract",
          "default": 100,
          "minimum": 1,
          "maximum": 1000
        },
        "includeFieldTypes": {
          "type": "boolean",
          "description": "Whether to detect and include field types",
          "default": true
        },
        "includeFormattedValues": {
          "type": "boolean",
          "description": "Whether to include formatted display values",
          "default": true
        },
        "waitForLoad": {
          "type": "number",
          "description": "Time to wait for table to load (ms)",
          "default": 3000
        }
      },
      "required": []
    },
    "toolInfo": {
      "emb": "Airtable table data extraction tool for public bases",
      "thought": "Manually copying data from Airtable loses structure and relationships. This tool extracts structured table data with field types and formatting in one operation.",
      "baseUrl": "https://airtable.com",
      "matchUrl": [
        "https://airtable.com/*",
        "https://airtable.com/*/tbl*",
        "https://airtable.com/shr*"
      ]
    }
  }